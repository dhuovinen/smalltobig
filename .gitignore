# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Config — contains API keys and local paths. The .example files are committed;
# real config is not. (*.env does not match *.env.example, so the templates are
# safe without a negation rule.)
.env
*.env

# Your data. Personal conversation logs and ingested copyrighted source text.
# Never commit these.
sources/
inbox/
*.db
*.sqlite3
data/

# macOS
.DS_Store
